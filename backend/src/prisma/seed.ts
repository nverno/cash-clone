import prisma from '../client';

async function main() {} // eslint-disable-line

main()
  .catch((e) => {
    console.error(e);
    process.exit(1);
  })
  .finally(async () => {
    await prisma.$disconnect();
  });
